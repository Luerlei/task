# 部门任务管理看板系统 — 实现计划

---

## 关键文件

| 文件 | 内容 | 预估规模 |
|------|------|----------|
| `package.json` | 项目依赖声明 | ~20行 |
| `server.js` | Express + 所有 API 路由 + lowdb 初始化 + JWT 中间件 | ~400行 |
| `public/index.html` | 完整前端：HTML + CSS 主题 + Vue 3 应用 | ~2000行 |
| `db.json` | 运行时自动生成的 lowdb 数据文件 | — |

---

## Phase 1：项目初始化与后端基础

| 步骤 | 内容 | 状态 |
|------|------|------|
| 1 | 创建 `package.json` | ✅ 完成 |
| 2 | 创建 `server.js`（Express + lowdb + JWT + 全部 API） | ✅ 完成 |

---

## Phase 2：前端实现

| 步骤 | 内容 | 状态 |
|------|------|------|
| 3 | 创建 `public/index.html`（完整前端） | ✅ 完成 |

---

## 启动方式

```bash
cd task
npm install
node server.js
```

浏览器访问 `http://localhost:3000`

---

## 验证清单

| # | 验证项 | 预期结果 |
|---|--------|----------|
| 1 | `npm install && node server.js` | 启动无报错，监听 3000 端口 |
| 2 | 浏览器访问 `http://localhost:3000` | 显示登录页 |
| 3 | admin / admin123 登录 | 登录成功，提示修改密码 |
| 4 | 管理员创建任务 | 卡片出现在"待认领"泳道 |
| 5 | 管理员创建普通成员账号 | 新用户能独立登录 |
| 6 | 普通成员登录 | 无创建任务/管理用户入口 |
| 7 | 普通成员认领任务 | assignees 列表更新 |
| 8 | 拖拽卡片到"进行中" | 状态更新，刷新后保持 |
| 9 | 多人认领同一任务 | assignees 正确包含多人 |
| 10 | 任务拖到"已完成" | 积分均分到认领者，排行榜更新 |
| 11 | 发表评论 | 评论显示在任务详情中 |
| 12 | 主题切换 | 所有组件颜色平滑过渡 |
| 13 | 另一台电脑通过局域网 IP 访问 | 数据与主机一致 |
